# AB Test Project
This project implements a simple A/B testing framework in Python.

## Usage
1. Create two variants of a page or feature.
2. Split traffic between the two variants.
3. Record engagement for each variant.
4. Get the results and statistical significance of the test.

## Running Tests
Run the tests using `python -m pytest` in the `tests` directory.
