# Code Catalyst Backend Scaffold Generator

## Usage
1. Run `python src/main.py` to generate the API scaffold.
2. Open `swagger.json` to view the API documentation.
3. Use the generated SDK to interact with the API.
