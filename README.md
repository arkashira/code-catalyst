# Code Catalyst
A drag-and-drop UI for building MVPs without coding.

## Features
* Add page templates
* Add components
* Reposition components
* Autosave and versioning

## Usage
1. Run `python -m pytest` to test the implementation.
2. Run `python src/code_catalyst.py` to use the Code Catalyst.
