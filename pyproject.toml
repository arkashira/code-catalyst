[project]
name = "code-catalyst-analytics"
version = "0.1.0"
description = "In‑memory analytics dashboard with CSV export for validation toolkit."
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
