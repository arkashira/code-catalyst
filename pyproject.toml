[project]
name = "code_catalyst_deployer"
version = "0.1.0"
description = "Automated staging deployment system for code-catalyst MVPs"
authors = ["Axentx OS <contact@axentx.com>"]
dependencies = []

[project.scripts]
deployer = "deployer:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
