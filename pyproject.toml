[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"

[tool.poetry]
name = "code-catalyst"
version = "1.0.0"
description = ""
authors = ["Your Name"]

[tool.poetry.dev-dependencies]
pytest = "^6.2.5"
