[project]
name = "code-catalyst"
version = "0.1.0"
description = "Simple MVP URL generator and copier for the Code Catalyst product."
authors = [{name = "Axentx"}]
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
